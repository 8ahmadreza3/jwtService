{
  "name": "tokenservice",
  "version": "1.0.0",
  "description": "use jsonwebtoken for validation and authorization users or another data",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "AhmadReza",
  "license": "ISC",
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  }
}
